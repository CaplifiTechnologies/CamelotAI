# Camelot website

Static product page and version stack. Open `index.html` locally or publish via GitHub Pages:

```bash
# From repo root — serve locally
python3 -m http.server 8765 --directory website
```

GitHub Pages: set source to `/website` on the `main` branch in repo Settings → Pages.